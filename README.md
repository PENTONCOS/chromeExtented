# chromeExtented

这个仓库作为多插件工作区使用，所有插件按目录隔离存放在 `plugins/` 下。

## 当前结构

- `plugins/codex-balance-monitor/`：Codex 余额监控插件（Cursor/VSCode）

## 新增插件建议

1. 在 `plugins/` 下新建独立目录
2. 每个插件维护自己的 `package.json`、依赖、构建产物
3. 在本文件补充插件目录索引
